<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Morson</title>
    <link rel="stylesheet" type="text/css" href="/Views/mystyles.css">
</head>
<body>

<header>
    <img src="/Views/Images/morson.png" alt="">
    <h1 style='position:relative; top:60px; left:-130px; font-size: 65px'>IT and Systems</h1>
</header>

<section>
    <div class="data-section">
        <h2>Scheduled Outages</h2>
        <?php if (isset($view->departmentDataSet['it_and_systems']['scheduled_outages'])): ?>
            <p><?php echo $view->departmentDataSet['it_and_systems']['scheduled_outages']; ?></p>
        <?php else: ?>
            <p>No scheduled outages data available.</p>
        <?php endif; ?>
    </div>

    <div class="data-section">
        <h2>System Status</h2>
        <?php foreach ($view->departmentDataSet['it_and_systems_software'] as $row): ?>
            <p><strong>Software:</strong> <?php echo $row['software']; ?></p>
            <p><strong>Build Status:</strong> <?php echo $row['build_status']; ?></p>
        <?php endforeach; ?>
    </div>

    <div class="data-section">
        <h2>Build Status - Software</h2>
        <?php foreach ($view->departmentDataSet['it_and_systems_status'] as $row): ?>
            <p><strong>System:</strong> <?php echo $row['system']; ?></p>
            <p><strong>System:</strong> <?php echo $row['status']; ?></p>
        <?php endforeach; ?>
    </div>
</section>

<footer>
    <p>sample text</p>
</footer>

<div class="disclaimer-section">
    <p>DISCLAIMER: [HackCamp Project]</p>
</div>

<!-- Add more sections or content as needed -->

</body>
</html>
<script>
    setTimeout(function(){
        window.location.reload(1);
    }, 30000);
</script>
